import React from "react";
import { Metadata } from "next";

type Props = {};

export const metadata: Metadata = {
  title: "About | Template Next-TS",
};

const About = (props: Props) => {
  return <h3>Select About</h3>;
};

export default About;
